///////////////////////////////////////////////////////////////
///                        T Y P E S                        ///
///////////////////////////////////////////////////////////////

export enum Move { Coöperate, Defect };

export type CandidateFunc = (myMoves: Move[], theirMoves: Move[]) => Move;
